import React from 'react';
import { shallow } from 'enzyme';
import StatePicker from './StatePicker';

describe('<StatePicker />', () => {
  it('Renders component', () => {
    const component = shallow(<StatePicker />);
    expect(false).toBeTruthy(); // Write the test
  });
});
