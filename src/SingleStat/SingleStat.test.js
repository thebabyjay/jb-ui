import React from 'react';
import { shallow } from 'enzyme';
import SingleStat from './SingleStat';

describe('<SingleStat />', () => {
  it('Renders component', () => {
    const component = shallow(<SingleStat />);
    expect(false).toBeTruthy(); // Write the test
  });
});
